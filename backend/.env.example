# ── Required ─────────────────────────────────────────────────────────
# Google Gemini API key. PydanticAI's GoogleProvider requires GOOGLE_API_KEY.
# The app automatically bridges GEMINI_API_KEY → GOOGLE_API_KEY on startup,
# so setting either one (or both) works.
GOOGLE_API_KEY=your-gemini-api-key
# GEMINI_API_KEY=your-gemini-api-key  # Alternative name (auto-bridged to GOOGLE_API_KEY)

# GitHub personal access token for profile ingestion
GITHUB_TOKEN=your-github-pat

# ── Database ─────────────────────────────────────────────────────────
# Local PostgreSQL for dev (default if NEON_DATABASE_URL is not set)
DATABASE_URL=postgresql+asyncpg://localhost:5432/minis
# Neon connection string (takes priority over DATABASE_URL when set)
# Format: postgresql+asyncpg://user:password@host/dbname?ssl=require
# NEON_DATABASE_URL=postgresql+asyncpg://neondb_owner:your-password@ep-your-endpoint-pooler.region.aws.neon.tech/neondb?ssl=require

# ── LLM & Model Hierarchy ────────────────────────────────────────────
# Active provider for all LLM calls (gemini | anthropic | openai)
DEFAULT_PROVIDER=gemini

# Model overrides per tier (optional, defaults provided):
# FAST_MODEL=google-gla:gemini-2.5-flash       # Summarization, compaction
# STANDARD_MODEL=google-gla:gemini-2.5-flash   # Explorers, chat
# THINKING_MODEL=google-gla:gemini-2.5-flash   # Soul synthesis
# EMBEDDING_MODEL=google-gla:text-embedding-004 # Vector embeddings

# ── Security ─────────────────────────────────────────────────────────
# JWKS URL for Neon Auth JWT validation (optional, for direct JWT validation)
NEON_AUTH_JWKS_URL=
# Secret key for JWT signing -- MUST change in production
JWT_SECRET=dev-secret-change-in-production
# Previous JWT secret for zero-downtime key rotation (optional)
# JWT_SECRET_PREVIOUS=
# Shared secret between BFF (Next.js) and backend for service JWTs
SERVICE_JWT_SECRET=dev-service-secret-change-in-production
# Secret for BFF→backend internal calls (e.g. /api/auth/sync) -- MUST change in production
INTERNAL_API_SECRET=dev-internal-secret-change-in-production
# Public GitHub OAuth App client ID used by CLI/MCP device auth.
# The OAuth app must allow GitHub's device flow.
# GITHUB_DEVICE_CLIENT_ID=your-github-oauth-client-id
# Explicit key material for encrypted user secrets. Required outside development.
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=

# ── Server ───────────────────────────────────────────────────────────
# Comma-separated CORS origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
DEBUG=true
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000

# ── Environment ──────────────────────────────────────────────────────
# Values: development | staging | production
ENVIRONMENT=development

# ── Admin ────────────────────────────────────────────────────────────
# Comma-separated GitHub usernames with admin access
ADMIN_USERNAMES=allie

# ── Observability (optional) ─────────────────────────────────────────
# Langfuse LLM tracing
# LANGFUSE_ENABLED=true
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_HOST=https://us.cloud.langfuse.com
