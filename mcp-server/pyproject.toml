[project]
name = "minis-mcp"
version = "0.1.0"
description = "MCP server for chatting with AI personality clones of GitHub developers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.5",
    "httpx>=0.28.1",
]
