# Frontend production environment variables
# When deployed to Vercel with API rewrites, this can be left empty (uses /api prefix).
# Set this to a full URL only if the backend is on a different domain without rewrites.
NEXT_PUBLIC_API_URL=
